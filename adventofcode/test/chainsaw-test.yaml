---
# yaml-language-server: $schema=https://github.com/kyverno/chainsaw/raw/refs/heads/main/pkg/data/schemas/json/test-chainsaw-v1alpha1.json
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test

metadata:
  name: adventofcode

spec:
  steps:
    - try:
        # First operation: create the Puzzle
        - apply:
            # Relative to the test folder
            file: 2015/1/puzzle.yaml

        # Second operation: verify the corresponding Answer exists and contains the expected data
        - assert:
            # Relative to the test folder
            file: 2015/1/answer.yaml
